---
// Decorative background layer. Stays hidden until the script takes it over, so
// nothing is announced and nothing is painted when JavaScript is unavailable.
---

<canvas class="pointer-events-none fixed inset-0 -z-10" data-dot-field aria-hidden="true" hidden
></canvas>
